import 'package:flutter/material.dart';

import 'package:get/get.dart';

class PromotionBodyView extends GetView {
  const PromotionBodyView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'PromotionBodyView is working',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
